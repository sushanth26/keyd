{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "sh -c 'npx prisma migrate deploy || echo migrate-failed; if [ \"$SEED_ON_BOOT\" = \"true\" ]; then SEED_SKIP_IF_POPULATED=true npm run db:seed || echo seed-failed; fi; npm run start'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
